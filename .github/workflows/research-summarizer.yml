name: "Research Synthesizer"

on:
  discussion:
    types: [labeled]

jobs:
  summarize:
    if: github.event.label.name == 'summarize'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run Summarizer Script
        run: |
          echo "⚙️ Hier käme ein Skript, das den Discussion-Inhalt holt, GPT-4 füttert und die Ergebnisse als .md-Dateien speichert."
          echo "Noch ist es ein Dummy. Ich bau dir das gern in Python oder Node, mit API-Key Support etc."
